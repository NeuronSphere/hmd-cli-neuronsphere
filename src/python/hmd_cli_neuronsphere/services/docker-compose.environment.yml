version: "3.7"

# One named local environment: a self-contained emulated AWS account.
#
# There is deliberately NO Floci or Postgres service here. Every environment is an emulated
# *account* inside the single control-plane Floci, selected per-request by the
# 12-digit account id used as the SigV4 access key
# (`floci_deployer.FlociTarget.access_key_id`) -- so its k3s/EKS cluster,
# Lambdas, buckets and secrets are all namespaced by account rather than
# separated by container. Its Postgres is likewise inside that account -- a
# Floci RDS instance deployed by `hmd-postgres-rds` in the environment's own core
# changeset, exactly as the cloud deploys it. What remains here is JanusGraph.
#
# NO service here publishes a host port. Everything is reached through
# `hmd_proxy`: HTTP at http://localhost/<slug>/<service>/. That is what allows
# several environments to run side by side.
#
# Every NS_ENV_* value comes from the environment registry
# (env_registry.LocalEnvironment.compose_env) and is exported by
# `environments.py` before compose is invoked. Container names are
# slug-suffixed because a Docker network alias must be unique on the shared
# network; inside each cluster CoreDNS maps the canonical names
# (hmd_db, global-graph) back to that environment's containers, so cloud Helm
# charts work unmodified.

# The graph is no longer here. It is a Floci Neptune cluster, provisioned
# lazily by the `hmd-inf-neptune` BOM entry only when something in the BOM
# declares a `database.neuronsphere.io/graph-database` dependency -- so a
# default `up` runs no graph at all, instead of a JVM per environment that most
# local work never touches. Floci spawns the same hmd-img-gremlin-server image
# this service used to run, and the CLI aliases that container as
# `global-graph-<slug>` so consumers are unaffected.
#
# This file now defines no services of its own; it is kept because the network
# declaration below is what `compose` needs to attach the project, and because
# a per-environment service may well come back.
services: {}

networks:
  neuronsphere_default:
    external: true
    name: ${NEURONSPHERE_DOCKER_NETWORK:-neuronsphere_default}
