version: "3.7"

services:
  proxy:
    container_name: hmd_proxy
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - neuronsphere_default
    volumes:
      - "${HMD_HOME}/.cache/nginx/neuronsphere.conf:/etc/nginx/nginx.conf:ro"

  db:
    container_name: hmd_db
    environment:
      HMD_CUSTOMER_CODE: ${HMD_CUSTOMER_CODE}
      HMD_DID: ${HMD_DID:-aaa}
      HMD_ENVIRONMENT: local
      HMD_REGION: ${HMD_REGION}
    image: ${HMD_LOCAL_NS_CONTAINER_REGISTRY:-ghcr.io/neuronsphere}/hmd-postgres-base:${HMD_POSTGRES_BASE_VERSION:-stable}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"
    networks:
      - neuronsphere_default
    volumes:
      - type: bind
        source: ${HMD_HOME}/postgresql/data
        target: /var/lib/postgresql/data
      # NOTE: extend mode intentionally does NOT mount postgres
      # docker-entrypoint-initdb.d scripts. Databases are created the
      # cloud-parity way — foundational control-plane DBs via
      # `ensure_core_databases_direct` (direct psql) and plugin DBs via the
      # dbaccount Lambda (`provision_plugin_databases`). Mounting the shared
      # `always-initdb.d` dir pulled in stale/broken scripts (e.g. an airflow
      # script with a bad interpreter) that abort the init chain and stall
      # postgres startup for minutes on a fresh boot.

  # Single Floci environment (control plane == workload account). The former
  # split admin/workload Floci instances were collapsed into this one; the
  # `neuronsphere-workload` network alias is retained so in-network consumers
  # that still address `neuronsphere-workload:4566` resolve to this instance.
  floci:
    image: floci/floci:1.5.8
    container_name: floci
    ports:
      - "4566:4566"
    environment:
      # Union of the former admin (eks) and workload (rds) service sets.
      FLOCI_SERVICES: s3,dynamodb,sqs,lambda,apigateway,iam,ecr,eks,secretsmanager,rds
      FLOCI_STORAGE_MODE: persistent
      FLOCI_LAMBDA_EXECUTOR: docker
      FLOCI_DEFAULT_REGION: ${AWS_REGION:-us-west-2}
      FLOCI_DEFAULT_ACCOUNT_ID: "000000000000"
      FLOCI_HOSTNAME: floci
      FLOCI_SERVICES_DOCKER_NETWORK: neuronsphere_default
      FLOCI_SERVICES_EKS_DOCKER_NETWORK: neuronsphere_default
      FLOCI_SERVICES_EKS_MOCK: "false"
      # Spawn k3s from the hmd-img-k3s-floci wrapper, whose entrypoint drops the
      # two broken kube-apiserver overrides Floci injects
      # (--kube-apiserver-arg=storage-backend=sqlite3 and etcd-servers=unix:///tmp/kine.sock)
      # that otherwise crash-loop the apiserver ("--storage-backend invalid").
      FLOCI_SERVICES_EKS_DEFAULT_IMAGE: ${HMD_LOCAL_K3S_WRAPPER_IMAGE:-${HMD_LOCAL_NS_CONTAINER_REGISTRY:-ghcr.io/neuronsphere}/hmd-img-k3s-floci:0.2}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-dummykey}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-dummykey}
    volumes:
      - "${HMD_HOME}/floci/data:/app/data"
      - "/var/run/docker.sock:/var/run/docker.sock"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:4566/_floci/health')"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s
    networks:
      neuronsphere_default:
        aliases:
          - neuronsphere
          - neuronsphere-workload

networks:
  neuronsphere_default:
    external: true
